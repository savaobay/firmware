iface wlan0 inet static
    address $(fw_printenv -n wlanaddr)
    netmask 255.255.255.0
    gateway $(fw_printenv -n wlangate)
    pre-up hostapd -B /etc/hostapd.conf
    pre-up sleep 1
    post-up udhcpd
    post-down killall -q udhcpd
    post-down killall -q hostapd
